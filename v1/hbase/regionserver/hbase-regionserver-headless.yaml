apiVersion: v1
kind: Service
metadata:
  name: hbase-regionserver-headless
spec:
  ports:
    - port: 16020
      name: hbase-regionserver-rpc
    - port: 16030
      name: hbase-regionserver-http
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: hbase-regionserver