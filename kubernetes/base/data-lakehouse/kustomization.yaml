apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # MinIO - Object Storage
  - minio-statefulset.yaml
  - minio-headless.yaml
  - minio-secret.enc.yaml
  # Trino - Query Engine
  - trino-statefulset.yaml
  - trino-headless.yaml
  - trino-configmap.yaml
  # Hive Metastore - Metadata Service
  - hive-metastore-statefulset.yaml
  - hive-metastore-headless.yaml
  - hive-metastore-configmap.yaml
  - hive-metastore-secret.enc.yaml

commonLabels:
  app.kubernetes.io/part-of: oneinchmarket
