apiVersion: v1
kind: Service
metadata:
  name: hive-metastore-headless
  labels:
    app.kubernetes.io/name: hive-metastore
    app.kubernetes.io/component: data-lakehouse
    app.kubernetes.io/part-of: oneinchmarket
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 9083
      targetPort: thrift
      protocol: TCP
      name: thrift
  selector:
    app.kubernetes.io/name: hive-metastore
