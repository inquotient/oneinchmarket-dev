apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Kafka KRaft
  - kafka/kafka-statefulset.yaml
  - kafka/kafka-headless.yaml
  - kafka/kafka-configmap.yaml
  # Apicurio Registry
  - apicurio/apicurio-statefulset.yaml
  - apicurio/apicurio-headless.yaml
  # Registry 웹 콘솔 — 3.x 는 UI 가 별도 이미지다. Studio 후계 화면이기도 하다.
  - apicurio/apicurio-ui-service.yaml
  - apicurio/apicurio-ui-deployment.yaml
  # - apicurio/apicurio-secret.enc.yaml  # SOPS: re-enable after ArgoCD SOPS plugin setup
  # Kafka Bridge - HTTP -> Kafka 단일 진입점 (ADR-032·033)
  - kafka-bridge/kafka-bridge-configmap.yaml
  - kafka-bridge/kafka-bridge-service.yaml
  - kafka-bridge/kafka-bridge-deployment.yaml
  # AKHQ
  - akhq/akhq-statefulset.yaml
  - akhq/akhq-headless.yaml
  - akhq/akhq-configmap.yaml

commonAnnotations:
  argocd.argoproj.io/sync-wave: "2"
