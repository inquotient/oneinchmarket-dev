apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Jenkins - ADR-022 로 복원. GitLab CI 와의 역할 분담은 TODO-40 미결
  - jenkins/jenkins-configmap.yaml
  - jenkins/jenkins-headless.yaml
  - jenkins/jenkins-statefulset.yaml
  - gitlab/gitlab-statefulset.yaml
  - gitlab/gitlab-headless.yaml
  - gitlab/gitlab-configmap.yaml
  # - gitlab/gitlab-secret.enc.yaml  # SOPS: re-enable after ArgoCD SOPS plugin setup

commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"
