apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kubescape-rbac.yaml
  - kubescape-cronjob.yaml

commonAnnotations:
  # 클러스터 전체를 스캔하므로 모든 워크로드가 올라온 뒤에 돈다.
  argocd.argoproj.io/sync-wave: "8"
