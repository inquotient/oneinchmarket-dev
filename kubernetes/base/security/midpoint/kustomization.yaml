# midPoint — IGA. WSO2-OSS-MAPPING Phase 2 ⑨.
# wave 4: PostgreSQL(1)·postgres-bootstrap 뒤, keycloak·openfga 와 같은 층.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - midpoint-deployment.yaml

commonAnnotations:
  argocd.argoproj.io/sync-wave: "4"
