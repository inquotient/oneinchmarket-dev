# OneinchMarket v2 - Prod Overlay
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Phase 4에서 추가:
# patches: []
# images: []   ← 이미지 태그 핀닝
# generators: []
