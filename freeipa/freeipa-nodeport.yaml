apiVersion: v1 
kind: Service
metadata: 
  name: freeipa-nodeport
spec: 
  ports: 
    - name: freeipa-http-port 
      port: 50080
      targetPort: 80
    - name: freeipa-https-port
      port: 50443
      targetPort: 443
  selector: 
    app: freeipa
  type: NodePort